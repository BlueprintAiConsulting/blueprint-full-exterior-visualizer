services:
  - type: web
    runtime: node
    name: blueprint-exterior-visualizer
    buildCommand: npm install
    startCommand: npx tsx server.ts
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: RESEND_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
